function loadData() {
    console.log('EisDebug @ saveManagement.js, addPlayerHTML() called.');
}

function updateAchievements() {
    // call if save version is different than game version
    // check all locked achievements once
    // add empty achievements to player object if more have been added
}